---
title: "数据库隔离级别"
type: concept
tags: [事务, 隔离级别, 并发]
sources:
  - "[[摘要-mysql-基础知识]]"
actionLink: "[[raw/09-archive/设计模式/raw/09-archive/mysql/基础知识/数据库事务]]"
last_updated: 2026-05-01
---

## 定义

数据库隔离级别定义了事务之间的隔离程度，用于平衡数据一致性和并发性能。SQL 标准定义了四种隔离级别，级别越高，并发性能越低，但数据一致性越好。

## 关键信息

### 并发问题

| 问题 | 说明 |
|------|------|
| 脏读 | 读取到其他事务未提交的数据 |
| 不可重复读 | 同一事务内多次读取同一数据返回不同结果（其他事务修改并提交） |
| 幻读 | 同一事务内多次查询返回不同数量的行（其他事务插入或删除） |

### 四种隔离级别

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 说明 |
|----------|------|------------|------|------|
| READ UNCOMMITTED | 可能 | 可能 | 可能 | 最低级别，任何情况都无法保证 |
| READ COMMITTED | 不可能 | 可能 | 可能 | 避免脏读 |
| REPEATABLE READ | 不可能 | 不可能 | 可能 | MySQL 默认级别 |
| SERIALIZABLE | 不可能 | 不可能 | 不可能 | 最高级别，性能最差 |

### MySQL 相关命令

```sql
-- 查看当前会话隔离级别
SELECT @@tx_isolation;

-- 查看系统当前隔离级别
SELECT @@global.tx_isolation;

-- 设置当前会话隔离级别
SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;

-- 设置系统当前隔离级别
SET GLOBAL TRANSACTION ISOLATION LEVEL REPEATABLE READ;
```

### 实现机制

- **READ UNCOMMITTED**：不加锁，直接读取最新数据
- **READ COMMITTED**：每次读取都创建新的 ReadView
- **REPEATABLE READ**：事务开始时创建 ReadView，整个事务期间使用同一个
- **SERIALIZABLE**：读加共享锁，写加排他锁

## 关联连接

- [[MySQL]] — 数据库系统
- [[InnoDB]] — 实现存储引擎
- [[ACID事务]] — 事务特性
- [[MVCC]] — 多版本并发控制
- [[redo-log]] — 重做日志
- [[摘要-mysql-基础知识]] — 来源
- [[raw/09-archive/设计模式/raw/09-archive/mysql/基础知识/数据库事务]] — 原始素材
